# Thread 'Compartment - superior agentic memory, encrypted at rest' in forum #community-projects-showcase
# thread_id=1528807731065262261 forum_id=1316137596535177246
# Started 2026-07-26T19:06:56.769515+00:00

[2026-07-26T16:12:21.072000+00:00] maxfreedompollard (id=1530971044486054048)
    Compartment - superior agentic memory, encrypted at rest

[2026-07-26T16:18:00.050000+00:00] maxfreedompollard (id=1530972466263097525)
    Added a topbar app for Mac and a minimal app for Windows. Very minimal, click to expand the menu. 
    
    I hated using other agentic memory, because it would inevitably unlink, and then I would just keep using the agent without knowing it was broken, wasting tons of time. Now I can click the topbar, see everything's running, toggle on options such as whether or not to save new memories, and view the last 5 memories saved at a quick glance. It also makes it easy to lock and unlock the encrypted memory.
    [attachment] Compartment.png https://cdn.discordapp.com/attachments/1528807731065262261/1530972465855987814/Compartment.png?ex=6a6784b7&is=6a663337&hm=582ae41948e5f9f838acf93f86d781c290e6f0a7cff128fb266357f46748dec1&

[2026-07-28T15:42:21.766000+00:00] maxfreedompollard (id=1531688273381036133)
    It now has a minimal (but feature packed) GUI on all 3 operating systems.

[2026-07-28T15:42:32.857000+00:00] maxfreedompollard (id=1531688319899926769)
    The superior math of Compartment, part 1 of 4: what gets stored.
    
    All of it lives in one file, src/compartment/ranking.py, which the vault, the dashboard and the benchmark all import, so a measurement is a measurement of the product and not of a copy that has drifted.
    
    THE PROBLEM. The encoder reads 512 tokens. Text past that is not weighted less, it is not seen at all, so a long memory was searchable only by its opening. On a real 6,705-memory vault, 40% of records ran past the window and 57.6% of the corpus was invisible to semantic search.
    
    THE FIX. A record is embedded as overlapping windows of W = 448 tokens at stride S = 384. The 64 tokens of overlap mean no fact is cut in half by a boundary. The record scores as its best window:
    
    windows(d) = ceil( max(0, tokens(d) - W) / S ) + 1     capped at 64
    
    s_vec(d)   = max over windows w of d of cos(q, w)
    
    In plain terms: chop a long memory into overlapping pages, and judge it by its best page.
    
    WHY MAX AND NOT AVERAGE. A memory is relevant if any part of it is relevant. Averaging punishes a long memory for the parts that are about something else. With one window per record the formula reduces exactly to the old behaviour, so it can never be worse for a short memory. The cost is small because most memories are short: 6,705 records produced 6,785 windows.
    
    Windows are counted in model tokens, never characters. A character budget is wrong by a factor of three between prose and a hex digest, and being wrong here means silently dropping the end of a memory.

[2026-07-28T15:42:41.219000+00:00] maxfreedompollard (id=1531688354972831795)
    Part 2 of 4: recall. Two indexes, combined as evidence rather than added.
    
    The vector index answers "what does this mean". The keyword index answers "what does this say". Their scores are not denominated in the same thing, and combining them is the entire difficulty.
    
    Adding them is the obvious move and it is wrong: a merely-good semantic match outvotes conclusive literal evidence. Searching a real vault for a commit sha occurring in exactly one memory out of 6,705 returned that memory below ten paraphrases of it. The keyword index had ranked it first and the sum buried it.
    
    The channels are not addends, they are alternatives: either one alone can establish relevance. That is a soft OR over independent evidence,
    
    P(relevant) = 1 - (1 - p_vec)(1 - p_lex)
    
    and the score is its logarithm, which ranks identically while still spreading results apart near the top instead of saturating at 1:
    
    score(d) = - w_vec log(1 - p_vec(d)) - w_lex log(1 - p_lex(d))
    w_vec = 0.75, w_lex = 0.25
    
    Either channel approaching certainty carries the memory on its own, and neither can veto the other.
    
    A COSINE AS A PROBABILITY. An L2-normalized encoder gives cosines comparable across queries, so fixed bounds work:
    
    p_vec(d) = clamp( (cos(q,d) - 0.25) / (0.85 - 0.25), 0, 0.88 )
    
    Per-query min-max normalization is the tempting alternative and it is a trap: it rescales the best hit of a hopeless query up to 1.0 and throws the calibration away.
    
    That 0.88 ceiling is load-bearing. A cosine is a similarity, never an identity: an encoder can say "this is about the same thing", never "this is the record you named". A literal match on a string unique to one memory can say exactly that. So the semantic channel is capped below the certainty the literal channel may reach, and the bound is forced, not chosen: the literal channel tops out at 0.25 * -log(1 - 0.999) = 1.727, so the cap must satisfy 0.75 * -log(1 - cap) < 1.727, giving cap < 0.90.

[2026-07-28T15:42:48.632000+00:00] maxfreedompollard (id=1531688386065072128)
    Part 3 of 4: reading a keyword hit as a probability, and deliberately not with BM25.
    
    BM25 answers "how well does this match", which is not what settles a contest against a semantic hit. What settles it is how unlikely the match was by chance. So each query term carries its self-information over the vault, and a memory scores the fraction of the query's information it accounts for:
    
    I(t)     = log( N / (1 + df(t)) )      N = records in the vault
    
    p_lex(d) = ( sum of I(t) over query terms t present in d ) / ( sum of I(t) over all query terms t )
    
    In plain terms: a word that appears in one memory out of 6,705 is almost proof; a word that appears in a tenth of them proves nearly nothing, whatever its BM25 happens to be. Scoring the share of the question's surprise that a memory accounts for is what makes a literal hit and a semantic hit comparable at all.
    
    The keyword index is queried as AND first, since an exact phrase match is the strongest signal available. FTS5's implicit AND means a nine-word question has to appear word for word, so when AND finds nothing it falls back to OR over only the terms carrying information: anything appearing in more than 10% of records is dropped. That ceiling is measured from your own vault rather than taken from an English stopword list, so it behaves the same for a vault full of code, of names, or of another language.
    
    Then a small rank-agreement residue, the one thing reciprocal-rank fusion is genuinely good at, sized to break ties rather than decide them:
    
    + w_rrf k [ 1/(k + rank_vec) + 1/(k + rank_lex) ]      w_rrf = 0.10, k = 20

[2026-07-28T15:42:54.535000+00:00] maxfreedompollard (id=1531688410824179884)
    Part 4 of 4: importance and recency multiply, they never add. And the numbers.
    
    final(d) = score(d) ( 1 + w_imp (2 importance(d) - 1) + w_rec 2^( -age_days(d) / 180 ) )
    
    w_imp = 0.15, w_rec = 0.10
    
    MULTIPLICATIVE, so a prior can only reorder a memory that already matched. An additive prior lets a very important memory surface for a question it has nothing to do with, which is how a memory system starts feeling haunted. A memory that matched nothing scores zero, and nothing can lift it off zero.
    
    CENTRED on the 0.5 default, which is why 2 importance - 1 appears rather than importance. Every unweighted memory carries 0.5, including thousands of starter facts. Uncentred, they all collect the same silent boost, which is another way of saying importance did nothing at all. Centred, an unweighted memory is exactly neutral and only a deliberate weight moves it.
    
    The tiers capture writes: decisions and consent 0.90, personal facts and preferences 0.80, machine and configuration 0.75, other substantive statements 0.55, pleasantries 0.20 (kept, ranked last). Recency halves every 180 days. Soon I'll make this recency reduction more elegant, I mention everything not just perfected, but this I'll fix once I figure something out.
    
    Namespace, tag, date and starter-fact filters run after ranking, so the candidate pool starts at 200 per channel and widens up to three times when filtering leaves too few.
    
    MEASURED end to end through Vault.search, on a real 6,705-memory vault, 44 queries in four families, against the previous scorer:
    
    Recall@1 0.523 to 0.773
    Recall@5 0.705 to 0.977
    MRR@10 0.601 to 0.845
    nDCG@10 0.627 to 0.878
    exact identifiers in top 5 4/10 to 10/10
    facts past the encoder window 0/6 to 5/6
    paraphrases 16/16 to 16/16
    median latency 4.4 ms to 11.6 ms

[2026-07-28T22:28:47.392000+00:00] viridis_48986 (id=1531790554109841479)
    @Max Freedom Pollard I read current public main at `3de423a` because the shared-vault security claim is exactly our lane. One concrete boundary before I would treat “stored data, not instructions” as enforcement: the MCP caller is declarative via `--caller`; the default ACL is wildcard read/write; Hermes hard-codes caller/namespace as `hermes`; and prefetch injects recalled text as bullets under a safety header. That is a coherent owner-local design, but cross-agent separation and prompt non-execution then depend on launch config and host obedience, not record-level origin proof. Is mutual trust across every connected agent the intended default, or should writes/recalls be origin-bound and default-deny? If useful, Viridis can do a bounded public-code review of just this boundary and return an evidence map plus one adversarial fixture—no install or paid call required to decide.

[2026-07-29T00:30:18.322000+00:00] jnwlsn__62097 (id=1531821134486700102)
    The crypto-shred claim is the one I'd want to test hardest. If a vault has been backed up or snapshotted before a memory is forgotten, does destroying the current record key also make those historical copies unreadable, or is the guarantee limited to the active vault?
    
    Also curious about the ‘stored data, not instructions’ boundary: have you run adversarial tests where a saved memory contains instruction-like text and later reaches multiple agents? Those two tests would make the security story much easier to trust.

[2026-07-30T12:56:01.148000+00:00] maxfreedompollard (id=1532371187366105189)
    The crux of what you're suggesting (correct me if I'm wrong) is that you're saying it would be better for each agent to hold a real credential and sign its calls, so every record proves which agent wrote it. That does not exist, but it's by design. I want it to be a central data storage, that all agents can use freely across fresh installs. I know I have installed and uninstalled hermes/claude/whatever countless times, and what I want for myself is a central encrypted storage that is just there, that works awesomely without config and that's encrypted. So I don't need to start from 0 whenever I tweak a config, and can add memory and knowledge to my AI. I also question the utility of agent-specific tagging. If someone was to inject fake data into the vault, wouldn't this agent string be just as easy to fake as anything else? The only security-improving way to introduce this would be some kind of secret from each agent to authenticate, but that would literally just be making the agents 2fa in essentially, which would be redonkulus.
    [reply to viridis_48986 msg=1531790554109841479]

[2026-07-30T13:01:35.153000+00:00] maxfreedompollard (id=1532372588284870797)
    To answer: If a memory was in the vault on Monday, you backed the vault up Tuesday, and you shredded the memory Wednesday, is that memory gone, or is it still sitting in Tuesday's copy?
    
    No, backups are not wiped or rendered null by design. They are backups, they're supposed to keep working if restored forever. 
    
    Each memory sits in its own locked box with its own key, and all those little keys are locked with your master key. 
    
    forget --shred does three things:
    
    Deletes the row, its search-index entries, and every embedding window, which destroys the wrapped per-record key along with the row.
    
    VACUUMs, so the freed SQLite pages holding the old text and its search tokens are not carried into the next image.
    
    Calls save(), which rewrites the entire vault file with an empty journal.
    
    The journal is a running log of recent operations appended to the vault file, and a store entry contains the memory's full text. Journal entries are sealed under the master key, not the per-record key. So burning the record key does nothing to a journal copy. Only the rewrite removes it.
    [reply to jnwlsn__62097 msg=1531821134486700102]

[2026-07-30T13:03:35.674000+00:00] maxfreedompollard (id=1532373093786456136)
    I would love you to do a code review of any part you want to do a code review of, by the way. I'm also happy to implement any security improvement. That is the intended default, but I could definitely find a way to do some agent-specific tagging of memories in a way that does not increase the recall speed or add any overhead. But it would look a lot like categorization, and wouldn't be user choosable or editable.
    [reply to viridis_48986 msg=1531790554109841479]

